bind generic 9 !@sh -c "git rev-parse --short %(commit) | pbcopy"
bind refs    R !git reset --hard %(commit)
bind refs    3 !git rebase -i %(branch)
bind refs    M !git merge %(branch)
bind generic W :!git reflog